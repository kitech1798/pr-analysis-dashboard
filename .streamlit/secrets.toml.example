# Streamlit 비밀 설정 — 이 파일은 템플릿입니다.
# 실제 사용 시 secrets.toml 로 복사 후 password 값을 변경하세요.
# .gitignore에 의해 secrets.toml 은 GitHub에 올라가지 않습니다.
#
# Streamlit Cloud 배포 후에는 이 값을 코드가 아닌
# Cloud 대시보드의 "Secrets" 메뉴에 붙여넣어 관리합니다.

password = "변경하세요"
